class ucar.nc2.stream.NcStreamReader$NetcdfFileStream extends ucar.nc2.NetcdfFile
{
    final ucar.nc2.stream.NcStreamReader this$0;

    private void <init>(ucar.nc2.stream.NcStreamReader)
    {
        ucar.nc2.stream.NcStreamReader$NetcdfFileStream r0;
        ucar.nc2.stream.NcStreamReader r1;

        r0 := @this: ucar.nc2.stream.NcStreamReader$NetcdfFileStream;

        r1 := @parameter0: ucar.nc2.stream.NcStreamReader;

        r0.<ucar.nc2.stream.NcStreamReader$NetcdfFileStream: ucar.nc2.stream.NcStreamReader this$0> = r1;

        specialinvoke r0.<ucar.nc2.NetcdfFile: void <init>()>();

        return;
    }

    void <init>(ucar.nc2.stream.NcStreamReader, ucar.nc2.stream.NcStreamReader$1)
    {
        ucar.nc2.stream.NcStreamReader$NetcdfFileStream r0;
        ucar.nc2.stream.NcStreamReader$1 r2;
        ucar.nc2.stream.NcStreamReader r1;

        r0 := @this: ucar.nc2.stream.NcStreamReader$NetcdfFileStream;

        r1 := @parameter0: ucar.nc2.stream.NcStreamReader;

        r2 := @parameter1: ucar.nc2.stream.NcStreamReader$1;

        specialinvoke r0.<ucar.nc2.stream.NcStreamReader$NetcdfFileStream: void <init>(ucar.nc2.stream.NcStreamReader)>(r1);

        return;
    }
}
