public abstract class org.apache.poi.poifs.filesystem.BlockStore extends java.lang.Object
{

    public void <init>()
    {
        org.apache.poi.poifs.filesystem.BlockStore r0;

        r0 := @this: org.apache.poi.poifs.filesystem.BlockStore;

        specialinvoke r0.<java.lang.Object: void <init>()>();

        return;
    }

    protected abstract int getBlockStoreBlockSize();

    protected abstract java.nio.ByteBuffer getBlockAt(int) throws java.io.IOException;

    protected abstract java.nio.ByteBuffer createBlockIfNeeded(int) throws java.io.IOException;

    protected abstract org.apache.poi.poifs.storage.BATBlock$BATBlockAndIndex getBATBlockAndIndex(int);

    protected abstract int getNextBlock(int);

    protected abstract void setNextBlock(int, int);

    protected abstract int getFreeBlock() throws java.io.IOException;

    protected abstract org.apache.poi.poifs.filesystem.BlockStore$ChainLoopDetector getChainLoopDetector() throws java.io.IOException;
}
